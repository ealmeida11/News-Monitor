# ── Dependências unificadas (News) ──────────────────────────────
# Compartilhadas
httpx
beautifulsoup4
requests
lxml
python-dotenv
websocket-client

# Pipeline (AI newsflow diário)
openai
pandas

# Dashboard / Pipeline (scrapers web)
selenium
webdriver-manager

# Realtime (monitor Edge CDP) — não precisa de selenium/webdriver-manager
